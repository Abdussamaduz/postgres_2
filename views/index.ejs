<%- include('parts/header') %>

<section class="pt-4 mt-4">
    <div class="container">
        <h1 class="display-2 text-center">Users</h1>
        <form class="my-4 py-4 row" action="/" method="POST">
            <label class="col-4">
                <input class="form-control" type="text" name="name" required placeholder="Name">
            </label>
            <label class="col-4">
                <input class="form-control" type="number" name="age" required placeholder="age">
            </label>
            <label class="col-4">
                <select name="referal" class="form-select">
                    <% for(let referal of referals) { %>
                        <option value="<%= referal.referal_id%>"><%= referal.referal_name%></option>
                    <% } %>
                </select>
            </label>
            <button class="btn col-12 btn-success mt-4">Add user</button>
        </form>
        <table class="mt-4 table w-100 fs-1 table-stiped table-hover table-bordered">
                <thead>
                    <tr>
                        <th>#id</th>
                        <th>name</th>
                        <th>age</th>
                        <th>Target</th>
                    </tr>
                </thead>
                <tbody>
                    <% for(let user of users){ %>
                        <tr>
                            <td><%= user?.user_id%></td>
                            <td class="d-flex justify-content-between align-items-center">
                                <%= user?.user_name %>
                                <a href="/delete/<%users?.user_id%>" type="submit" class="btn btu mx-4 btn-danger">del</a>
                            </td>
                            <td><%= user?.user_age%></td>
                            <td><%= user?.referal_name%></td>
                        </tr>
                    <% } %>
                    
                </tbody>
        </table>
    </div>
</section>

<%- include('parts/footer') %>